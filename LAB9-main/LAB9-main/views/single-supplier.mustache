
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A layout example with a side menu that hides on mobile, just like the Pure website.">
    <title>Northwind | {{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

    <h1>{{ title }}</h1>

    <form action="/suppliers/{{ supplier.id }}" method="POST">

        <table>
            <thead>
                <tr>
                    <th>Property</th>
                    <th>Value</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Company</td>
                    <td>
                        <input type="text" name="company" value="{{ supplier.company }}" />
                    </td>
                </tr>
                <tr>
                    <td>First Name</td>
                    <td>
                        <input type="text" name="first_name" value="{{ supplier.first_name }}" />
                    </td>
                </tr>
                <tr>
                    <td>Last Name</td>
                    <td>
                        <input type="text" name="last_name" value="{{ supplier.last_name }}" />
                    </td>
                </tr>
                <tr>
                    <td>Job Title</td>
                    <td>
                        <input type="text" name="job_title" value="{{ supplier.job_title }}" />
                    </td>
                </tr>
            </tbody>
        </table>

        <button class="btn btn-primary" type="submit">
            Save Changes
        </button>

    </form>
    <hr />
    
    <a href="/suppliers/" class="btn btn-secondary">
        Back to list of suppliers
    </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
